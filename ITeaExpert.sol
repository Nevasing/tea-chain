// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

/**
 * @title ITeaExpert (заглушка)
 * @dev ВЕРСИЯ 0.1 — ПРОЕКТ ЗАКРЫТ В 2025 ГОДУ
 * 
 * ПОЧЕМУ? Блокчейн не решает проблему физического чая, но идеален для архива знаний.
 * 
 * Этот контракт НЕ РЕАЛИЗОВАН, потому что:
 * 1. Нет массового спроса на децентрализованную верификацию экспертов;
 * 2. Эксперты не используют кошельки;
 * 3. Невозможность отслеживать состояние физического чая через контракт; 
 * 4. Реальная проблемиа — подделки (блокчейн здесь бессилен).
 * 
 * Полная история: docs/LESSONS.md
 */
contract ITeaExpert {
    // Проект закрыт — контракт не реализован
}
